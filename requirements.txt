# Core runtime. The CLI itself needs only Typer; Docker is invoked as a subprocess
# and SQLite/xml/tarfile are stdlib.
typer>=0.12

# LLM provider SDKs are optional - install the one you plan to use, or
# `pip install -e ".[all]"`. Uncomment as needed:
# boto3>=1.34       # --provider bedrock
# anthropic>=0.40   # --provider anthropic
# openai>=1.40      # --provider openai
# google-genai>=0.5 # --provider gemini
# datasets>=2.19    # evals scaffold --instance-id
